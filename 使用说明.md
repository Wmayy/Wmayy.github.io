# 双色球助手 - 手机端应用

一款可以安装到手机主屏幕的 PWA 应用，提供双色球开奖查询、扫码验票、智能选号等功能。

## 功能特性

### 1. 最新开奖
- 自动获取福利彩票双色球最新开奖号码
- 显示期号、开奖日期、奖池信息
- 支持手动刷新，离线时使用本地缓存数据

### 2. 扫码验票
- 调用手机后置摄像头扫描彩票二维码/条码
- 自动识别彩票号码并比对最新开奖结果
- 支持手动输入号码作为备选
- 显示中奖等级、命中个数、兑奖提示

### 3. 智能选号
- 基于近50期历史数据的冷热号分析
- 趋势加权算法，倾向温号（避免极端冷热）
- 可配置过滤条件：
  - 过滤掉往期中奖号码
  - 排除最大连号（4个及以上连续）
  - 排除全奇/全偶组合
- 支持生成 3/5/10 注推荐号码

### 4. 历史开奖
- 内置50期历史开奖数据
- 支持分页加载查看
- 本地缓存，离线可用

### 5. 手动选号
- 可视化号码球选择器
- 支持机选、清空、保存
- 保存的选号本地存储

## 手机安装方法

### Android（Chrome 浏览器）
1. 用 Chrome 打开应用网址
2. 点击右上角菜单（三个点）
3. 选择「添加到主屏幕」
4. 确认后即可在桌面找到应用图标

### iOS（Safari 浏览器）
1. 用 Safari 打开应用网址
2. 点击底部分享按钮（方框+向上箭头）
3. 选择「添加到主屏幕」
4. 确认后即可在桌面找到应用图标

### 安装后体验
- 全屏运行，无浏览器地址栏
- 支持离线使用（Service Worker 缓存）
- 启动画面和原生应用一致

## 本地运行

### 方法一：Python 简易服务器
```bash
cd 双色球助手
python -m http.server 8080
```
然后浏览器访问 `http://localhost:8080`

### 方法二：Node.js
```bash
npx serve 双色球助手 -p 8080
```

### 方法三：直接部署
将整个文件夹上传到任意静态托管服务：
- GitHub Pages
- Vercel / Netlify
- 阿里云 OSS / 腾讯云 COS
- 自己的服务器

> 注意：PWA 功能（安装到桌面、离线缓存、相机调用）需要 HTTPS 或 localhost 环境才能正常工作。

## 数据来源说明

- 应用内置50期历史开奖数据作为离线兜底
- 在线时尝试从公开 API 获取最新开奖数据
- 如网络获取失败，自动使用本地缓存数据
- 奖池金额为估算值，仅供参考

## 技术栈

- 纯前端实现，无需后端服务器
- HTML5 + CSS3 + 原生 JavaScript
- PWA（Manifest + Service Worker）
- jsQR 二维码识别库
- 本地存储（localStorage）

## 目录结构

```
双色球助手/
├── index.html          # 主页面
├── manifest.json       # PWA 配置
├── service-worker.js   # 离线缓存
├── css/
│   └── style.css       # 样式表
├── js/
│   ├── app.js          # 主应用逻辑
│   ├── lottery.js      # 彩票核心算法
│   └── scanner.js      # 扫码模块
└── icons/
    ├── icon-192.png    # 应用图标
    └── icon-512.png    # 应用图标
```

## 免责声明

本应用仅供学习和娱乐使用，开奖数据以中国福利彩票官方公布为准。
彩票有风险，投注需理性，请量力而行。
